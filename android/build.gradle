buildscript {
    ext {
        buildToolsVersion = "36.0.0"
        minSdkVersion = 24
        compileSdkVersion = 36
        targetSdkVersion = 36
        ndkVersion = "27.1.12297006"
        kotlinVersion = "2.1.20"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle")
        classpath("com.facebook.react:react-native-gradle-plugin")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin")
    }
}

apply plugin: "com.facebook.react.rootproject"

// Add this block below the one above
allprojects {
    repositories {
        mavenCentral()
        maven {
            url 'https://api.mapbox.com/downloads/v2/releases'
            authentication {
                basic(BasicAuthentication)
            }
            credentials {
                // Do not change the username to your Mapbox username.
                username = 'mapbox'
                // Paste your Mapbox access token as the password
                password = "pk.eyJ1Ijoicm9oaXRrb2xsYSIsImEiOiJjbDF2ajF4d3AwMGNvM2RwYXFmbG44Z3huIn0.H7rC0mC_P9NqU97n_wVw8w"
            }
        }
    }
}